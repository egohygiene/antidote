{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Deny-by-default permissions for the Antidote mock session interface",
  "windows": ["main"],
  "permissions": ["core:default"]
}
