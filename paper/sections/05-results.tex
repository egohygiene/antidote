% Copyright 2026 Alan Szmyt. All rights reserved pending publication license.
\section{Results}
\label{sec:results}

This design/protocol manuscript contains no formal human results. No qualifying
formal-study or H1 package is registered. No qualifying T1 package is
registered; therefore no real-model technical result is reported. The
originating catharsis experience remains a hypothesis-generating observation
rather than a study result. These statements describe the governed repository
record, not events that might have occurred outside the protocol. The absence
of a qualifying record is reported as \emph{not run}, \emph{not collected},
\emph{blocked}, or \emph{not applicable}; it is never entered as a zero.

Results reporting is governed by \texttt{ANT-REPORT-RESULTS-001}, version~1.1.0.
It preserves the original issue~\#41 governance and records issue~\#82 as the
prospective pre-collection amendment. The machine-readable contract binds each
future result slot to a named source record, the byte-locked
\texttt{ANT-PROT-FEAS-001} protocol version~1.1.0, a frozen analysis pointer, an
evidence class, and a claim-promotion rule. It currently registers no T0, T1,
or H1 result package. This structure adopts the complete-accounting orientation
of N-of-1 and pilot-feasibility reporting guidance---including participant
flow, missingness, harms, analysis, and progression---without describing
Antidote as a completed trial or effectiveness study
\cite{vohra2015cent,eldridge2016feasibility}.

\subsection{Technical verification reporting}
\label{sec:results-technical-verification}

The D0 protocol record is available for inspection. Its identity and content
lock are repository artifacts, while the reporting contract declares the
intended slot, schema, equation, and activation-gate mappings. This is evidence
that versioned design records and explicit mappings exist; it is not an
execution result, an independent audit of implementation, or evidence of
feasibility, privacy, safety, or benefit.

The repository also contains an executable synthetic session path and its
component tests, as bounded by ANT-OBS-002. Those artifacts remain development
and conformance evidence. The ANT-Q03 exit gate in issue~\#18 has not produced
the reserved \texttt{ANT-REC-T0-001} result package, so no T0 rate, count,
latency, or success claim is promoted here. Closing an issue or observing a
green workflow would not be sufficient by itself. Promotion requires the
reviewed source revision, execution environment, exact commands, planned
denominators, every terminal state, injected failure and recovery, artifact
hashes, exclusions, and an explicit claim-ledger decision. Any accepted T0
statement must retain the labels \emph{synthetic} and \emph{technical} and
cannot imply a human exposure or response.

No interface capture is included in the current manuscript because the
qualifying \texttt{ANT-REC-T0-001} package does not exist. The governed visual
slot is retained as retired provenance so a later revision can reactivate it
only with a reproducible reviewed build, capture metadata, an explicit
synthetic-or-approved-data label, and redaction review. Interface presence
could demonstrate that a control is displayed; it could not demonstrate that
the underlying authority is complete or that an experience is helpful.

\subsection{Control adherence and continuity}
\label{sec:results-control-adherence}

T1 remains \texttt{blocked-no-real-model}. No T1 qualification manifest has
frozen a specific model, adapter, analyzer, hardware profile, technical
threshold set, or run plan, and \texttt{ANT-REC-T1-001} does not exist.
Consequently, the governed
table below contains source and analysis identities plus current evidence
states, but no attempt accounting, authority or artifact-integrity invariant
results, requested controls, measured features, adherence values, latencies,
buffer margins, boundary distances, failure rates, or uncertainty estimates.

\AntidoteTable{control-adherence-results}
\FloatBarrier

If T1 is later activated, its matrix reaches 39 attempts only when every
declared control is supported; otherwise reporting will name each unsupported
or missing attempt and the resulting denominator. Authority and artifact-integrity
invariants will each be reported as pass, fail, or unavailable rather than
being inferred from an aggregate success state. Requested-versus-measured
differences will be paired by factor and seed; generation latency and verified
buffer margin will name the hardware context; semantic and waveform boundary
components will remain separate; and partial output, timeout, cancellation,
failure, and recovery will stay in their denominators. Unsupported semantic
controls will be reported as unsupported rather than assigned an invented
acoustic measure. A technically accurate tempo, sufficient buffer, or small
waveform boundary does not establish perceived continuity, affective fit,
safety, or therapeutic value.

No benchmark graphic is included until the T1 source package and analysis
outputs pass promotion review. Its governed visual slot is retained as retired
provenance. A future reactivation must show denominators, uncertainty, missing
runs, failures, model and hardware revisions, and source-package and analysis
identities rather than only favorable traces.

\subsection{Exposure and response completeness}
\label{sec:results-exposure-completeness}

H1 remains \texttt{blocked-no-collection-authority}; none of its activation
gates is satisfied and no assigned attempt, generated study artifact, exposure,
or response record is registered. The table shell names the future flow,
immediate-response, later-response, cross-package provenance, and progression
analyses. Its blocked cells are not denominators and must not be read as zero
events.

\AntidoteTable{exposure-response-completeness}
\FloatBarrier

After separately authorized collection, reporting would begin with every
assigned attempt and retain the transitions to attempted generation, verified
artifact, started playback, completed exposure, immediate window, and later
window. Conditions G, S, and P would be shown separately and together. Generated
but unplayed audio would remain outside the exposure denominator; an interrupted
session would remain in the attempt ledger; a replacement would not erase the
original assignment; and withdrawal would not be converted into technical
failure. Each response field would be classified as present or with exactly one
canonical reason: \texttt{not\_prompted}, \texttt{declined},
\texttt{missed\_window}, \texttt{technical\_failure},
\texttt{interrupted}, or \texttt{not\_applicable}. The wanted-intensity and
ongoing-effect responses of \emph{unsure} are present answers, not missingness.

The progression report would show every criterion as pass, fail, or
indeterminate and would keep audit completeness separate from usable voluntary
response completeness. An explicit decline can complete the audit trail but
cannot be counted as a response value. The public H1 audit envelope would also
have to reconcile the flow, response, and safety envelopes before any H1 slot
could be promoted. A stop, revise, or proceed disposition concerns only a later
bounded protocol and cannot establish advisory usefulness, improved targeting,
efficacy, privacy, generalizability, or safety.

\subsection{Subjective response and aftereffect}
\label{sec:results-subjective-response}

No perceived-expression, felt-state, helpfulness, resonance, mismatch, harm,
surprise, burden, or aftereffect observation is reported. The reporting contract
reserves six distinct H1 analysis views: perceived musical expression versus
felt state; baseline-to-immediate felt-state change; immediate appraisal by
condition; exact randomization uncertainty; prespecified sensitivities; and
immediate versus later aftereffect. The first preserves both reports without
declaring either one correct. The second uses only paired present baseline and
immediate valence or arousal values. The third retains every session point and
prespecified within-block P-minus-S, P-minus-G, and S-minus-G contrast in the
protected analysis output, while the public envelope exposes privacy-reviewed
aggregate summaries only. These compare bundled conditions and cannot isolate
the effect of personalization, context access, or plan editing. The sixth
retains the immediate and later records as separate window-specific protected
accounts rather than using later meaning to rewrite the immediate response.

Future public summaries must display exact aggregate denominators,
prespecified medians, ranges, and blockwise summaries; session-level values
remain in protected analysis output. The
exact randomization distribution is eligible only if all six blocks satisfy its
frozen analysis conditions. It enumerates the 720 assignments of the six unique
condition sequences to the six chronological blocks; it is exploratory
within-person uncertainty, not a population estimate or confirmatory test. No
clinical outcome is collected, and movement toward a desired state cannot
substitute for the person's appraisal of whether an experience was wanted,
helpful, mismatched, burdensome, or harmful.

Any populated report must also retain the prespecified sensitivity views:
started exposures versus those with at least 80 percent of audio heard, with
versus without carryover flags, blocks one through three versus four through
six, all six leave-one-block-out summaries, administration status only if both
statuses occur, and the declared lower- and
upper-bound burden decisions. These are reporting views rather than
opportunities to select a preferred estimate after observing the data. A bound
that changes the decision makes that criterion indeterminate and the final
disposition \emph{revise}; it is not resolved in the preferred direction. None
has been computed.

\subsection{Null, negative, missing, and adverse observations}
\label{sec:results-nonpositive-observations}

The empty-state table gives null or opposite-direction response, mismatch or
unwanted intensity, burden, harm or adverse response, missingness, and
interruption the same structural visibility as a future favorable observation.
It contains no event counts because H1 has not begun.

\AntidoteTable{outcomes-accounting}
\FloatBarrier

In a qualifying report, zero movement would remain distinct from a missing
measurement; opposite-direction movement would remain distinct from harm;
mismatch would remain distinct from unwanted intensity; \emph{unsure} would
remain distinct from missing wanted intensity or ongoing effect; and \texttt{declined} would
remain distinct from \texttt{missed\_window} or
\texttt{technical\_failure}. Stops,
interruptions, hearing discomfort, distress, consent or privacy failures,
technical safety failures, and participant-described adverse responses would
be listed with their denominators and review dispositions. None could be
silently removed because it weakens a narrative. Conversely, the absence of an
observed adverse event in a bounded series could not establish clinical safety.

\subsection{Optional physiological observations}
\label{sec:results-optional-physiology}

Physiology is disabled under protocol version~1.1.0. No physiological source
record, analysis, panel, or result row is authorized, and no vital sign or
sensor estimate may steer generation within this protocol. A future module
would require a protocol amendment, separate consent, device and clock records,
signal-quality and artifact handling, a frozen analysis, and independent
privacy and safety review. Even then, physiology would remain supplemental: it
could not diagnose emotion or need, overwrite self-report, demonstrate
entrainment, or establish benefit.

\subsection{Promotion and replacement rule}
\label{sec:results-promotion-rule}

The current Results state is a governed empty-state specification: its three
tables are deterministic version~1.1 projections bound to the final protocol
lock, while the two evidence-contingent visual slots are retired and absent
from the manuscript. To replace an empty state, a future change must add
the reserved source package, validate its protocol and analysis identity,
preserve complete denominators and non-positive records, register any
deviations, and record a claim-ledger promotion decision. The reporting
contract and generated projection must then be versioned together. A source
package may remain available in the repository while its manuscript claim is
rejected or downgraded.

Every H1 repository source is a privacy-reviewed aggregate envelope with a
\texttt{.public.json} suffix. The flow, response, safety, and audit envelopes
must share one evidence-set identity and opaque, nonce-bound integrity commitment. Public
envelopes may contain aggregate accounting, prespecified summaries,
uncertainty, public-safe artifacts, and review dispositions; they may not
contain participant, session, consent-grant, or source-event identifiers, raw
or free-text responses, row-level observations, private paths, per-record
hashes, stable cross-package pseudonyms, or participant-event timestamps. The
private evidence index and commitment nonce remain outside Git. Their
commitment supports reconciliation only; it does not prove anonymity, consent,
authenticity, truth, completeness, efficacy, or safety.

This gate is deliberately asymmetric: qualifying evidence may populate only
the slot it supports, while failure at any provenance, authority, analysis, or
claim boundary leaves the slot unavailable. Technical verification cannot
unlock human-response claims, H1 feasibility cannot establish efficacy, and no
result at these stages can establish a neurological mechanism.
